project(util)
file(GLOB UTIL_SOURCES "*.cc")
if (WIN32)
	file(GLOB UTIL_SOURCES_IMPLS "mswin/*.cc")
else()
	file(GLOB UTIL_SOURCES_IMPLS "posix/*.cc")
endif()
include_directories("${SDL2_INCLUDE_DIR}")
add_library(${PROJECT_NAME} STATIC ${UTIL_SOURCES} ${UTIL_SOURCES_IMPLS})
